# Android / Gradle
*.iml
.gradle
/build
**/build/
/captures
.externalNativeBuild
.cxx
*.hprof

# IDE
/.idea/
.DS_Store

# Local machine / SDK paths (never commit)
local.properties

# Keystores & signing (never commit)
*.jks
*.keystore
*.pepk
signing.properties
keystore.properties

# Firebase / Google Services — contains API keys and project identifiers
# Copy from Firebase Console → Project settings → Your apps → google-services.json
app/google-services.json
google-services*.json
!app/google-services.json.example

# Node (if present at project root)
node_modules/
npm-debug.log*

# Env and generic secrets
.env
.env.*
!.env.example

# Service account keys (if ever added)
*-firebase-adminsdk-*.json
serviceAccount*.json

# OS / misc
Thumbs.db
*.swp
*~
